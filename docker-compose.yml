version: '3.8'
services:
  app1:
    build: .
    container_name: app1
  app2:
    build: .
    container_name: app2
  nginx:
    image: nginx:alpine
    depends_on: [app1,app2]
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
